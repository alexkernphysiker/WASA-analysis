cmake_minimum_required(VERSION 2.6)
project(WASAatCOSY_etamesic_3He_search_analysis)
set(CMAKE_CXX_FLAGS "-std=gnu++11")
if(NOT $ENV{ROOTSYS} STREQUAL "")
	include_directories($ENV{ROOTSYS}/include/)
	link_directories($ENV{ROOTSYS}/lib/)
	set(ROOT_LIBRARIES Core Gui Gpad Graf Hist RIO Physics Tree)
	add_subdirectory(WASA-libs)
	include_directories(WASA-libs/FitGen/include/)
	include_directories(WASA-libs)
	add_subdirectory(He3Eta)
endif()
